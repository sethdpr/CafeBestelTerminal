<Window x:Class="CafeBestelTerminal.Views.BestellingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bestellingdetails" Height="340" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Bestelling" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
            <WrapPanel Margin="0,2">
                <TextBlock Text="Naam:" Width="120"/>
                <TextBlock Text="{Binding Naam}"/>
            </WrapPanel>
            <WrapPanel Margin="0,2">
                <TextBlock Text="Datum:" Width="120"/>
                <TextBlock Text="{Binding Datum, StringFormat={}{0:g}}"/>
            </WrapPanel>
            <WrapPanel Margin="0,2">
                <TextBlock Text="Personeelslid:" Width="120"/>
                <TextBlock Text="{Binding Personeelslid}"/>
            </WrapPanel>
            <WrapPanel Margin="0,2">
                <TextBlock Text="Klant:" Width="120"/>
                <TextBlock Text="{Binding Klant.Naam}"/>
            </WrapPanel>
            <WrapPanel Margin="0,2">
                <TextBlock Text="Totaalprijs:" Width="120" FontWeight="Bold"/>
                <TextBlock Text="{Binding Totaalprijs, StringFormat=€{0:F2}}" FontWeight="Bold"/>
            </WrapPanel>
        </StackPanel>

        <GroupBox Header="Producten" Grid.Row="1" Margin="0,8,0,8">
            <ItemsControl ItemsSource="{Binding BestellingProducten}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel LastChildFill="False" Margin="0,2">
                            <TextBlock Text="{Binding Aantal}" Width="30"/>
                            <TextBlock Text="x" Width="10" TextAlignment="Center"/>
                            <TextBlock Text="{Binding Product.Naam}" Width="220"/>
                            <TextBlock Text="{Binding Product.Prijs, StringFormat=€{0:F2}}" Width="80" TextAlignment="Right"/>
                            <TextBlock Text="{Binding Totaalprijs, StringFormat=€{0:F2}}" Width="80" FontWeight="Bold" TextAlignment="Right"/>
                        </DockPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Verwijder" Width="100" Click="Verwijder_Click"/>
        </StackPanel>
    </Grid>
</Window>
